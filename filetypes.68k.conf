# For complete documentation of this file, please see Geany's main documentation
[styling]
# Edit these in the colorscheme .conf file instead
default=default
comment_single=#008000
comment=#008000
commentblock=#008000
commentdirective=comment
number=#800080
string=string_1
operator=operator
identifier=identifier_1
cpuinstruction=#800080
mathinstruction=keyword_2
register=type
directive=preprocessor
directiveoperand=keyword_3
character=character
stringeol=string_eol
extinstruction=keyword_4

[keywords]
# all items must be in one line
# this is by default a very simple instruction set; not of Intel or so
instructions=add.b add.w add.l adda.w adda.l addi.b addi.w addi.l addq.b addq.w addq.l addx.b addx.w addx.l and.b and.w and.l andi.b andi.w andi.l asl.b asl.w asl.l asr.b asr.w asr.l 
registers=a0 a1 a2 a3 a4 a5 a6 a7 sp d0 d1 d2 d3 d4 d5 d6 d7 sr usp pc ccr
directives=org list include incbin equ macro endm dc.b dc.w dc.l

[settings]
# default extension used when saving files
extension=68k
lexer_filetype=ASM
tag_parser=ASM


# the following characters are these which a "word" can contains, see documentation
wordchars=_abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789
whitespace_chars=\s\t!\"%&'()*+,-./:;<=>?@[\\]^`{|}~

# single comments, like # in this file
comment_single=;
# multiline comments
#comment_open=
#comment_close=

# set to false if a comment character/string should start at column 0 of a line, true uses any
# indentation of the line, e.g. setting to true causes the following on pressing CTRL+d
	#command_example();
# setting to false would generate this
#	command_example();
# This setting works only for single line comments
comment_use_indent=true

# context action command (please see Geany's main documentation for details)
context_action_cmd=

[indentation]
#width=4
# 0 is spaces, 1 is tabs, 2 is tab & spaces
#type=1

[build_settings]
# %f will be replaced by the complete filename
# %e will be replaced by the filename without extension
# (use only one of it at one time)
compiler=asmx -C 68000 -e -w -b 0 -l out/rom.lst -o out/rom.bin -- "%f"
#compiler=asmx "%f"

